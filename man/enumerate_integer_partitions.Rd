% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumerate_integer_partitions.R
\name{enumerate_integer_partitions}
\alias{enumerate_integer_partitions}
\title{Enumerate All Integer Partitions}
\usage{
enumerate_integer_partitions(n_items, n_clusters)
}
\arguments{
\item{n_items}{Integer specifying the total number of items being partitioned
(the integer partition sums to \code{n_items}). Use \code{n_items = 0} only with
\code{n_clusters = 0}, which returns a single empty integer partition.}

\item{n_clusters}{Integer specifying the number of clusters (parts) in each integer partition.
Use \code{n_clusters = 0} only when \code{n_items = 0}.}
}
\value{
A matrix where each row represents a unique integer partition. The matrix
has \code{n_clusters} columns, with elements in non-decreasing order within each row.
The number of rows equals the number of integer partitions of \code{n_items} into exactly
\code{n_clusters} positive parts.
}
\description{
Enumerates all possible integer partitions of \code{n_items} into exactly \code{n_clusters}
positive parts in non-decreasing order. This is useful for computing exact
probabilities over the entire support of the Lorenz-IP distribution.
}
\details{
An integer partition of \eqn{n} into \eqn{k} parts is a sequence
\eqn{(x_1, x_2, \ldots, x_k)} of positive integers satisfying:
\itemize{
\item \eqn{x_1 + x_2 + \cdots + x_k = n} (parts sum to n)
\item \eqn{x_1 \le x_2 \le \cdots \le x_k} (non-decreasing order)
\item \eqn{x_i \ge 1} for all \eqn{i} (all parts are positive)
}

The number of such integer partitions grows rapidly with \code{n_items} and \code{n_clusters}.
For large values, this function may consume significant memory and time.
}
\examples{
# Enumerate all integer partitions of 10 into 3 parts
partitions <- enumerate_integer_partitions(10, 3)
print(partitions)

# Each row sums to n_items
rowSums(partitions)

# Each row is in non-decreasing order
all(apply(partitions, 1, function(x) all(diff(x) >= 0)))

# Number of integer partitions grows with n_items and n_clusters
nrow(enumerate_integer_partitions(10, 2))
nrow(enumerate_integer_partitions(10, 3))
nrow(enumerate_integer_partitions(10, 4))
nrow(enumerate_integer_partitions(20, 4))

# Use with dlorenzip to compute exact probabilities over all integer partitions
n_items <- 10
n_clusters <- 3
weights <- c(1, 2, 3)
concentration <- 5

all_partitions <- enumerate_integer_partitions(n_items, n_clusters)
probs <- dlorenzip(all_partitions, n_items, weights, concentration = concentration)

# Probabilities sum to 1
sum(probs)

# Find the most likely integer partition
all_partitions[which.max(probs), ]
}
