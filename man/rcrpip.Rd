% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rlorenzip.R
\name{rcrpip}
\alias{rcrpip}
\title{Sample from the Chinese Restaurant Process Integer Partition}
\usage{
rcrpip(n, n_items, n_clusters, discount)
}
\arguments{
\item{n}{Integer specifying the number of samples to generate.}

\item{n_items}{Integer specifying the total number of items being partitioned
(the integer partition sums to \code{n_items}).}

\item{n_clusters}{Integer specifying the number of clusters in the integer partition.}

\item{discount}{Numeric value in [0, 1) specifying the discount parameter for
the CRP. Larger values favor more clusters (and relatively smaller cluster
sizes); discount = 0 recovers the Ewens/CRP case.}
}
\value{
A matrix with \code{n} rows and \code{k} columns (even when \code{n = 1}), where
each row represents a sampled integer partition. Each element indicates the
number of items assigned to the corresponding cluster, in non-decreasing
order (smallest to largest).
}
\description{
Generates random samples from the integer partition distribution of the Chinese Restaurant Process (CRP)
}
\examples{
# Sample integer partitions from the CRP (Ewens case, discount = 0)
set.seed(42)
samples <- rcrpip(n = 5, n_items = 20, n_clusters = 4, discount = 0)
print(samples)

# With discount > 0 (Pitman-Yor case)
samples_py <- rcrpip(n = 5, n_items = 20, n_clusters = 4, discount = 0.25)
print(samples_py)
}
