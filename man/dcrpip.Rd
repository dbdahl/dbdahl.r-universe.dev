% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcrpip.R
\name{dcrpip}
\alias{dcrpip}
\title{Probability Mass Function of the CRP Integer Partition Distribution}
\usage{
dcrpip(x, n_items, n_clusters, discount, log = FALSE)
}
\arguments{
\item{x}{A matrix with \code{n} rows and \code{k} columns, where each row represents an
integer partition. Each element indicates the number of items assigned to the
corresponding cluster, in non-decreasing order (smallest to largest). For
convenience, decreasing-order integer partitions are accepted and are sorted internally.
A vector of length \code{k} is also permissible.}

\item{n_items}{Integer specifying the total number of items being partitioned
(the integer partition sums to \code{n_items}).}

\item{n_clusters}{Integer specifying the number of clusters in the integer partition.}

\item{discount}{Numeric value in \verb{[0, 1)} specifying the discount parameter.}

\item{log}{Logical; if \code{TRUE}, returns the natural logarithm. Default is \code{FALSE}.}
}
\value{
A numeric vector of length \code{n}.
}
\description{
Evaluates the probability mass function (PMF) for integer partitions under
the two-parameter Chinese Restaurant Process (CRP), conditional on the number
of clusters.
}
\details{
For an integer partition with sizes \verb{n_1, ..., n_k} and multiplicities \code{r_j} of equal sizes,
the conditional PMF is
\deqn{P(n_1, ..., n_k | n, k, \sigma) = \frac{n!}{\prod_i n_i! \prod_j r_j!}
  \frac{\prod_i (1-\sigma)_{n_i-1}}{S_\sigma(n, k)}}{P = [n! / (prod n_i! prod r_j!)] * prod (1-sigma)_{n_i-1} / S_sigma(n, k)}

where \verb{S_\\sigma(n, k)} is the generalized Stirling number. Invalid integer partitions
return 0 (or \code{-Inf} if \code{log = TRUE}).
}
\examples{
n_items <- 20
k <- 4
discount <- 0.2

set.seed(42)
samples <- rcrpip(5, n_items, k, discount)
print(samples)

probs <- dcrpip(samples, n_items, k, discount)
print(probs)

log_probs <- dcrpip(samples, n_items, k, discount, log = TRUE)
print(log_probs)

all.equal(exp(log_probs), probs)

single_partition <- c(2, 4, 6, 8)
prob_single <- dcrpip(single_partition, n_items, k, discount)
print(prob_single)

invalid_partition <- c(2, 4, 6, 7)
prob_invalid <- dcrpip(invalid_partition, n_items, k, discount)
print(prob_invalid)
}
\seealso{
\code{\link[=rcrpip]{rcrpip()}} for sampling from this distribution, \code{\link[=dcrpk]{dcrpk()}} for \code{P(K = k)}.
}
