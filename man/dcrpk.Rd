% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcrpk.R
\name{dcrpk}
\alias{dcrpk}
\title{Probability Mass Function of the Number of Clusters from a Two-Parameter CRP}
\usage{
dcrpk(x, n_items, concentration, discount, log = FALSE)
}
\arguments{
\item{x}{Integer vector of values at which to evaluate the PMF. Values must be
between 1 and \code{n_items} inclusive; values outside this range return 0 (or \code{-Inf}
if \code{log = TRUE}).}

\item{n_items}{Integer specifying the number of items (customers) in the set partition.}

\item{concentration}{Numeric value specifying the CRP concentration (mass)
parameter alpha, must be greater than \code{-discount}. For the standard
one-parameter CRP (Dirichlet process), alpha must be positive. Note: this
is the CRP parameter alpha, not the Lorenz-IP concentration gamma.}

\item{discount}{Numeric value in \verb{[0, 1)} specifying the discount parameter.
When \code{discount = 0}, this reduces to the standard one-parameter CRP.}

\item{log}{Logical; if \code{TRUE}, returns log-probabilities. Default is \code{FALSE}.}
}
\value{
A numeric vector of the same length as \code{x} containing the (log-)probabilities.
}
\description{
Evaluates the probability mass function (PMF) for the number of clusters (tables)
when sampling from the two-parameter Chinese Restaurant Process (CRP), also known
as the Pitman-Yor process.
}
\details{
The PMF for the number of clusters \code{K} in a sample of \code{n} items from a
two-parameter CRP with concentration \code{alpha} and discount \code{sigma} is:

\deqn{P(K = k | n, \alpha, \sigma) = \frac{S_\sigma(n, k) \cdot C(\alpha, \sigma, k)}{(\alpha+1)_{n-1}}}{P(K = k | n, alpha, sigma) = S_sigma(n, k) * C(alpha, sigma, k) / (alpha+1)_(n-1)}

where:
\itemize{
\item \code{S_sigma(n, k)} is the generalized unsigned Stirling number of the first kind
\item \code{C(alpha, sigma, k) = (alpha + sigma) * (alpha + 2*sigma) * ... * (alpha + (k-1)*sigma)}
\item \verb{(alpha+1)_(n-1)} is the rising factorial (Pochhammer symbol)
}

For \code{discount = 0} (standard CRP), this simplifies to:
\deqn{P(K = k | n, \alpha) = \frac{|s(n, k)| \cdot \alpha^{k-1}}{(\alpha+1)_{n-1}}}{P(K = k | n, alpha) = |s(n, k)| * alpha^(k-1) / (alpha+1)_(n-1)}

where \verb{|s(n, k)|} is the unsigned Stirling number of the first kind.

All computations are performed in log-space for numerical stability, making
this function suitable for large \code{n_items} (tested up to 100,000).
}
\examples{
# PMF for standard CRP
k_values <- 1:20
probs <- dcrpk(k_values, n_items = 50, concentration = 2, discount = 0)
barplot(probs, names.arg = k_values, xlab = "Number of clusters", ylab = "Probability")

# Verify probabilities sum to 1
sum(dcrpk(1:50, n_items = 50, concentration = 2, discount = 0))

# Log-probabilities for numerical stability with large n_items
log_probs <- dcrpk(1:100, n_items = 1000, concentration = 5, discount = 0.25, log = TRUE)

# Compare CRP vs Pitman-Yor
k <- 1:30
p_crp <- dcrpk(k, n_items = 100, concentration = 1, discount = 0)
p_py <- dcrpk(k, n_items = 100, concentration = 1, discount = 0.5)
plot(k, p_crp, type = "h", col = "blue", lwd = 2,
     xlab = "Number of clusters", ylab = "Probability")
lines(k + 0.2, p_py, type = "h", col = "red", lwd = 2)
legend("topright", c("CRP (d=0)", "Pitman-Yor (d=0.5)"), col = c("blue", "red"), lwd = 2)
}
\seealso{
\code{\link[=rcrpk]{rcrpk()}} for sampling from this distribution.
}
