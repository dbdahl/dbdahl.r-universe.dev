% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lorenz.R
\name{lorenz_ispline}
\alias{lorenz_ispline}
\title{Create an I-Spline Lorenz Curve}
\usage{
lorenz_ispline(partition, n_interior_knots = NULL, degree = 2L, lambda = 1e-06)
}
\arguments{
\item{partition}{Numeric vector of positive values representing cluster sizes
or weights. The partition does not need to be sorted; functions that take
partitions sort to non-decreasing order internally.}

\item{n_interior_knots}{Number of interior knots for the I-spline basis.
If \code{NULL} (default), automatically set to \code{max(1, min(length(partition) - 2, 10))}.}

\item{degree}{Degree of the I-spline (default 2, giving cubic splines).}

\item{lambda}{Roughness penalty weight (default 1e-6). Larger values give
smoother curves.}
}
\value{
An external pointer object of class \code{c("lorenz_ispline", "lorenz")}
representing the Lorenz curve.
}
\description{
Constructs a smooth I-spline Lorenz curve fitted to an empirical partition.
The curve is constrained to be monotonically increasing and pass through
(0, 0) and (1, 1).
}
\examples{
# Create a Lorenz curve from an empirical partition
partition <- c(1, 2, 3, 4, 5)

# Piecewise linear Lorenz curve
curve_linear <- lorenz_linear(partition)
print(curve_linear)

# I-spline Lorenz curve (smooth)
curve_ispline <- lorenz_ispline(partition)
print(curve_ispline)

# Evaluate curves at specific points
x <- seq(0, 1, by = 0.25)
lorenz_evaluate(curve_linear, x)
lorenz_evaluate(curve_ispline, x)

# Compute Gini coefficient
gini(curve_linear)
gini(curve_ispline)

# Direct Gini computation from partition (without creating curve)
gini_coefficient(partition)

# Extract Lorenz curve increments for a different number of clusters
lorenz_increments(curve_linear, n_clusters = 3)
lorenz_increments(curve_ispline, n_clusters = 3)

# Use Lorenz curve with rlorenzip
set.seed(42)
samples <- rlorenzip(
  n = 5,
  n_items = 20,
  target = curve_linear,
  n_clusters = 5,
  concentration = 10
)
print(samples)

# Plot a Lorenz curve (if graphics available)
if (interactive()) {
  plot(curve_linear)
  plot(curve_ispline)
}
}
\seealso{
\code{\link[=lorenz_linear]{lorenz_linear()}} for piecewise linear Lorenz curves,
\code{\link[=gini]{gini()}} for computing the Gini coefficient,
\code{\link[=lorenz_increments]{lorenz_increments()}} for extracting Lorenz curve increments.
}
